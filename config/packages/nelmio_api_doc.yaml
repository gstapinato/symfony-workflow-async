nelmio_api_doc:
    documentation:
        info:
            title: Demo batch processing
            description: 
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
        disable_default_routes: true
    use_validation_groups: true
    